<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="16dp"
        android:paddingVertical="20dp">

        <Button
            android:id="@+id/open_progress_screen"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/main_open_progress_bar"
            app:layout_constraintTop_toTopOf="parent" />

        <Button
            android:id="@+id/open_chooser_screen"
            android:layout_width="match_parent"
            android:layout_marginTop="16dp"
            android:layout_height="wrap_content"
            android:text="@string/main_open_chooser"
            app:layout_constraintTop_toBottomOf="@id/open_progress_screen" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</ScrollView>